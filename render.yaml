services:
  - type: web
    name: video-parser-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    instanceType: free
    healthCheckPath: /api/health
    envVars:
      - key: VOLC_APP_ID
        sync: false
      - key: VOLC_ACCESS_TOKEN
        sync: false
